# kubernetes
Here with in this part we are installing kubernetes cluster version 1.30 on centos/stream9 operating system. 
There are basically two ways to install the 3 nodes k8s cluster.[1 master node & 2 worker nodes.]
    1. We can install the cluster using automated way using vagrant files. Just run the below command to run the k8s cluster.
        # vagrant up [This will provision the kubernetes cluster with one master & two worker nodes.]
    2. We can use below commands to install the cluster in manual ways.
    

